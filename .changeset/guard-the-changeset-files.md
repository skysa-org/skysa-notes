---

---

Nothing ships in this change. `.changeset` joins the control-byte guard's walked
list, which is a test asking a question about files rather than a change to any
of them — and an empty changeset is the honest answer to "what does this do to
the version?", rather than a bump invented to satisfy the question.
