---
'@skysa/web': patch
---

The `/` menu now closes when what you've typed after the slash matches no
command. `/nothing` used to leave an empty box under the cursor. Delete back
to a query that matches something and the menu opens again.
