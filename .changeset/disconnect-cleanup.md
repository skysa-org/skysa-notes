---
---

Nothing ships in this change. The three changes that moved a disconnect off the
device's own pile left nothing dead behind them in the code — a build that fails
on an unused declaration sees to that — so what was left was to say the same
thing in the plan and in one comment that still described the panel as it was
before it held more than one account.
