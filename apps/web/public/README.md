App icons here are generated placeholders (`scripts/` has no generator checked
in; they were produced once from the shapes in `favicon.svg`). Replace them with
real artwork before any public launch — see `TRADEMARK.md` on branding.
